apiVersion: v1
kind: Service
metadata:
  name: magalabs-logprint-svc
spec:
  type: NodePort
  selector:
    app: magalabs-logprint
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30000
